# I Need Help - 实用工具集合

一个基于 Rust 开发的实用工具集合项目，包含多个独立的小工具应用。

## 项目结构

```
ineedhelp/
├── apps/                    # 应用程序目录
│   ├── jsonfmt/            # JSON 格式化工具
│   └── cryptotool/         # 加密工具（开发中）
├── Cargo.toml              # 工作空间配置
└── README.md               # 本文件
```

## 工具列表

### 1. JSON 格式化工具 (jsonfmt)

一个功能完整的 JSON 格式化、压缩和编辑工具。

**主要功能：**
- JSON 格式化与压缩
- 树形结构展示
- 行号显示
- 搜索与编辑
- 剪贴板集成

**详细文档：** [apps/jsonfmt/README.md](apps/jsonfmt/README.md)

**运行：**
```bash
cargo run --release --bin jsonfmt
```

### 2. 加密工具 (cryptotool)

🚧 开发中...

## 快速开始

### 环境要求

- Rust 1.70 或更高版本
- Windows 10 或更高版本

### 安装 Rust

访问 [https://rustup.rs/](https://rustup.rs/) 安装 Rust 工具链。

### 克隆项目

```bash
git clone <repository-url>
cd ineedhelp
```

### 构建所有工具

```bash
# 开发构建
cargo build

# 发布构建（优化版本）
cargo build --release
```

### 运行特定工具

```bash
# 运行 JSON 格式化工具
cargo run --release --bin jsonfmt

# 运行加密工具（开发中）
cargo run --release --bin cryptotool
```

## 工作空间配置

本项目使用 Cargo 工作空间管理多个应用程序，共享依赖配置。

### 共享依赖

- **eframe** 0.33.0 - GUI 框架
- **serde_json** 1.0.145 - JSON 处理
- **serde** 1.0.228 - 序列化框架
- **arboard** 3.6.1 - 剪贴板支持
- **egui_json_tree** 0.14.2 - JSON 树形展示

### 发布配置优化

项目配置了激进的发布优化选项：

- **opt-level = 3** - 最高性能优化
- **lto = "fat"** - 完整链接时优化
- **codegen-units = 1** - 单个代码生成单元
- **strip = true** - 移除调试符号
- **panic = "abort"** - 减小二进制体积

这些配置可以显著减小最终可执行文件的体积并提升性能。

## 添加新工具

1. 在 `apps/` 目录下创建新的应用目录：
   ```bash
   cargo new apps/newtool
   ```

2. 在根 `Cargo.toml` 中添加到工作空间：
   ```toml
   [workspace]
   members = [
       "apps/jsonfmt",
       "apps/newtool",  # 新工具
   ]
   ```

3. 在新工具的 `Cargo.toml` 中使用工作空间依赖：
   ```toml
   [dependencies]
   eframe.workspace = true
   serde.workspace = true
   ```

## 构建输出

发布构建的可执行文件位于：

```
target/release/
├── jsonfmt.exe
└── cryptotool.exe
```

## 技术栈

- **语言**: Rust
- **GUI 框架**: eframe/egui
- **构建系统**: Cargo (工作空间模式)

## 开发指南

### 代码风格

项目遵循 Rust 标准代码风格，使用 `rustfmt` 格式化代码：

```bash
cargo fmt
```

### 代码检查

使用 `clippy` 进行代码质量检查：

```bash
cargo clippy --all-targets --all-features
```

### 测试

运行所有测试：

```bash
cargo test --workspace
```

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
